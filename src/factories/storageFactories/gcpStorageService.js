exports.storageService = {
    getFile: async function(fileInfo) {
        // Code for fetching file from GCP
        return true;
    },
    
    saveFile: async function(file) {
        // Code for uploading file to GCP
        return true;
    },

    deleteFile: async function(file) {
        // Code for deleting file from GCP
        return true;
    }
}